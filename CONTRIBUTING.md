# Contributing to Shelly Scripts Backup

Спасибо за интерес к проекту! Мы приветствуем вклад от сообщества.

## Как внести вклад

### Сообщить об ошибке

1. Проверьте, не была ли уже создана похожая проблема в [Issues](https://github.com/yourusername/shelly_scripts_backup/issues)
2. Используйте шаблон Bug Report
3. Предоставьте как можно больше деталей:
   - Версия Home Assistant
   - Версия интеграции
   - Модель устройства Shelly
   - Версия прошивки Shelly
   - Логи из Home Assistant
   - Шаги для воспроизведения

### Предложить новую функцию

1. Проверьте [Issues](https://github.com/yourusername/shelly_scripts_backup/issues) на наличие похожих предложений
2. Используйте шаблон Feature Request
3. Опишите проблему, которую решает ваша идея
4. Предложите возможное решение

### Отправить Pull Request

1. Fork репозиторий
2. Создайте новую ветку для вашей функции или исправления:
   ```bash
   git checkout -b feature/amazing-feature
   ```
   или
   ```bash
   git checkout -b fix/bug-description
   ```

3. Внесите изменения:
   - Следуйте стилю кода проекта
   - Добавьте комментарии к сложной логике
   - Обновите документацию, если необходимо

4. Зафиксируйте изменения:
   ```bash
   git commit -m 'Add some amazing feature'
   ```
   Используйте [Conventional Commits](https://www.conventionalcommits.org/):
   - `feat:` для новых функций
   - `fix:` для исправлений
   - `docs:` для изменений в документации
   - `style:` для форматирования
   - `refactor:` для рефакторинга
   - `test:` для тестов
   - `chore:` для рутинных задач

5. Push в ваш fork:
   ```bash
   git push origin feature/amazing-feature
   ```

6. Создайте Pull Request с описанием изменений

## Стандарты кода

### Python код

- Следуйте [PEP 8](https://www.python.org/dev/peps/pep-0008/)
- Используйте type hints где возможно
- Добавляйте docstrings к функциям и классам
- Максимальная длина строки: 100 символов

Пример:
```python
async def backup_scripts(self) -> None:
    """Backup all scripts from the device.
    
    This method retrieves all scripts from the Shelly device
    and saves them to the backup directory.
    """
    # Implementation
```

### Форматирование

Используйте следующие инструменты:
```bash
# Black для форматирования
black custom_components/shelly_scripts_backup/

# isort для сортировки импортов
isort custom_components/shelly_scripts_backup/

# pylint для проверки
pylint custom_components/shelly_scripts_backup/
```

### Тестирование

- Добавляйте тесты для новой функциональности
- Убедитесь, что все существующие тесты проходят
- Покрытие кода тестами должно быть максимальным

```bash
pytest tests/
```

## Структура коммитов

### Хороший коммит:
```
feat: add support for script scheduling backup

- Add new service for scheduling backups
- Update documentation
- Add tests for scheduler

Closes #123
```

### Плохой коммит:
```
fixed stuff
```

## Процесс ревью

1. После создания PR, один или несколько мейнтейнеров проверят ваш код
2. Могут быть запрошены изменения
3. После одобрения, PR будет смержен
4. Ваш вклад появится в следующем релизе

## Вопросы?

Если у вас есть вопросы:
- Создайте [Discussion](https://github.com/yourusername/shelly_scripts_backup/discussions)
- Напишите в [Home Assistant Community Forum](https://community.home-assistant.io/)

## Кодекс поведения

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Никакого harassment или hate speech
- Следуйте [Code of Conduct](https://www.contributor-covenant.org/)

## Лицензия

Внося вклад, вы соглашаетесь с тем, что ваш вклад будет лицензирован под MIT License.

---

**Спасибо за ваш вклад! 🎉**
